{
  "name": "glyco-balance-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html *.css *.js *.png *.jpg *.svg *.woff dist/ 2>/dev/null; true"
  }
}
